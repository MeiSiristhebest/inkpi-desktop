/** Desktop re-exports the protocol contract so proposal validation cannot drift. */
export { validateDomainProposal } from '@inkpi/protocol'
export type { DomainProposal, DomainProposalEvidence } from '@inkpi/protocol'
